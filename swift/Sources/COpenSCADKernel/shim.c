/* Intentionally empty. The COpenSCADKernel target exists only to expose the
 * openscad_kernel.h C ABI as a Clang module; the implementation lives in the
 * prebuilt libopenscad_kernel.a (built by CMake) and is linked by consumers. */
